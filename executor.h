#ifndef __EXECUTOR_H__
#define __EXECUTOR_H__
#include "data_translation.h"

///Parcels will have three steps that can be more or less be executed directly on the shell
int execute_data_parcel(struct data_parcel *parcel);

#endif

